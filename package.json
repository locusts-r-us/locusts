{
  "name": "locusts-monorepo",
  "version": "0.0.0",
  "devDependencies": {
    "prettier": "^3.2.5",
    "syncpack": "13.0.0",
    "turbo": "^2.1.2",
    "typescript": "^5.6.2"
  },
  "engines": {
    "node": ">=18"
  },
  "private": true,
  "scripts": {
    "build": "turbo build",
    "dev": "turbo dev",
    "format": "prettier --write \"**/*.{ts,tsx,md}\"",
    "lint": "turbo lint",
    "test": "turbo test"
  }
}
